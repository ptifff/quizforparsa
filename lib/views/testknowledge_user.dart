
import 'package:flutter/material.dart';

class TestKnowledgeUser extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Test Knowledge For User'),
      ),
      body: Center(
        child: Text('This is the Test Knowledge screen For User'),
      ),
    );
  }
}
