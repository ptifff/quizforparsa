
import 'package:flutter/material.dart';

class GatherKnowledgeUser extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gather Knowledge For User'),
      ),
      body: Center(
        child: Text('This is the Gather Knowledge screen For User'),
      ),
    );
  }
}
