
import 'package:flutter/material.dart';

class GatherKnowledgeAdmin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gather Knowledge For Admin'),
      ),
      body: Center(
        child: Text('This is the Gather Knowledge screen For Admin'),
      ),
    );
  }
}
